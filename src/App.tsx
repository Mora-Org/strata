function App() {
  return (
    <main className="min-h-screen p-8" style={{ background: 'var(--bedrock)', color: 'var(--fg-1)' }}>
      <h1 className="text-3xl">Strata</h1>
      <p className="mt-2 text-sm opacity-60">M1.a — foundation scaffolding</p>
      <p className="mt-1 text-xs opacity-40">
        Vite + React 19 + TS + Tailwind v3 + Vitest. Token bridge ativo (design/colors_and_type.css).
      </p>
    </main>
  );
}

export default App;

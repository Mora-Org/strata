# OS
.DS_Store
Thumbs.db
desktop.ini

# Design artifacts — codificação acontece em M0.5.c. Até lá, ficam local.
# Backup vive em ~/Documents/strata-design-backups/ (fora do repo).
*Strata Design System*

# Safety net — qualquer .zip no repo. Se precisar versionar um zip
# específico no futuro, override com `!nome-especifico.zip`.
*.zip

# Future code (preempt pra evitar commits acidentais quando M1 começar)
node_modules/
dist/
build/
out/
.env
.env.local
.env.*.local
*.log
npm-debug.log*

# Tauri
src-tauri/target/

# Editor
.vscode/
.idea/
*.swp
*.swo
